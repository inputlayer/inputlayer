% Test: Exactly two hops

.kg create test_two_hop_n09t91
.kg use test_two_hop_n09t91

+edge[(1, 2), (2, 3), (3, 4), (1, 3)].

% Exactly two hops
+two_hop(X, Z) :- edge(X, Y), edge(Y, Z).

?- two_hop(X, Z).

.kg use default
.kg drop test_two_hop_n09t91
