% Test: Exactly two hops
% Expected: Path of exactly length 2
% Category: 09_recursion
% Priority: P2 - Recursion patterns

.kg create test_two_hop
.kg use test_two_hop

+edge[(1, 2), (2, 3), (3, 4), (1, 3)].

% Exactly two hops
+two_hop(X, Z) :- edge(X, Y), edge(Y, Z).

?- two_hop(X, Z).

.kg use default
.kg drop test_two_hop
