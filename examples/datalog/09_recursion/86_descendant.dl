% Test: Descendant relation
% Expected: All descendants of a node
% Category: 09_recursion
% Priority: P2 - Recursion patterns

.kg create test_descendant
.kg use test_descendant

+parent[(1, 2), (1, 3), (2, 4), (2, 5), (3, 6)].

% Direct child is descendant
+descendant(P, C) :- parent(P, C).

% Transitive
+descendant(P, D) :- parent(P, C), descendant(C, D).

% All descendants of 1
?- descendant(1, D).

.kg use default
.kg drop test_descendant
