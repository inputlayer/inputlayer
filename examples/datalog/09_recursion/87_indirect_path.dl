% Test: Indirect path finding
% Expected: Path via intermediate nodes
% Category: 09_recursion
% Priority: P2 - Recursion patterns

.kg create test_indirect
.kg use test_indirect

+direct[(1, 2), (2, 3), (3, 4), (1, 4)].

% Indirect path (at least 2 hops)
+indirect(X, Z) :- direct(X, Y), direct(Y, Z).

?- indirect(X, Z).

.kg use default
.kg drop test_indirect
