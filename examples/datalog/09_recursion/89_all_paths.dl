% Test: All paths enumeration
% Expected: Find all possible paths
% Category: 09_recursion
% Priority: P2 - Recursion patterns

.kg create test_all_paths
.kg use test_all_paths

+edge[(1, 2), (2, 3), (1, 3)].

% All paths
+path(X, Y) :- edge(X, Y).
+path(X, Z) :- path(X, Y), edge(Y, Z).

?- path(X, Y).

.kg use default
.kg drop test_all_paths
