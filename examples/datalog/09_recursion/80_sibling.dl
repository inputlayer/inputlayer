% Test: Sibling relation

.kg create test_sibling_n09t80
.kg use test_sibling_n09t80

+parent[(1, 2), (1, 3), (1, 4), (2, 5), (2, 6)].

% Siblings share a parent (excluding self)
+sibling(X, Y) :- parent(P, X), parent(P, Y), X < Y.

?- sibling(X, Y).

.kg use default
.kg drop test_sibling_n09t80
