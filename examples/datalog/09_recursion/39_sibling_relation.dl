% Test: Sibling relation
% Expected: Find nodes with same parent
% Category: 09_recursion
% Priority: P2 - Recursion patterns

.kg create test_sibling
.kg use test_sibling

+parent[(1, 2), (1, 3), (1, 4), (2, 5), (2, 6)].

% Siblings: same parent, different nodes
sibling(A, B) :- parent(P, A), parent(P, B), A < B.

?- sibling(A, B).

.kg use default
.kg drop test_sibling
