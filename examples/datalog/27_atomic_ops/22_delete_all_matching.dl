% Test: Delete all matching a condition
% Expected: Bulk delete with condition
% Category: 27_atomic_ops
% Priority: P2 - Atomic operations

.kg create test_del_all
.kg use test_del_all

+items[(1, "A", 100), (2, "B", 200), (3, "A", 150), (4, "C", 50)].

?- items(Id, Cat, Val).

% Delete all category A items
-items(Id, Cat, Val) :- items(Id, Cat, Val), Cat = "A".

?- items(Id, Cat, Val).

.kg use default
.kg drop test_del_all
