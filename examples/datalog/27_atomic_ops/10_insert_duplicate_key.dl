% Test: Insert duplicate key behavior
% Expected: Handle duplicate key insertion
% Category: 27_atomic_ops
% Priority: P2 - Atomic operations

.kg create test_dup_key
.kg use test_dup_key

+data[(1, "first"), (2, "second")].

?- data(X, Y).

% Insert with same key
+data[(1, "updated")].

?- data(X, Y).

.kg use default
.kg drop test_dup_key
