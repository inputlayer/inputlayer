% Test: Conditional delete with no matches
% Expected: Nothing deleted when condition matches nothing
% Category: 27_atomic_ops
% Priority: P1 - Atomic operations

.kg create test_cond_del_empty
.kg use test_cond_del_empty

+data[(1, "a"), (2, "b"), (3, "c")].

% Empty condition - nothing to delete
+to_delete[(99)].

?- data(Id, Val).

% Try to delete based on empty match
-data(Id, Val) :- to_delete(Id), data(Id, Val).

?- data(Id, Val).

.kg use default
.kg drop test_cond_del_empty
