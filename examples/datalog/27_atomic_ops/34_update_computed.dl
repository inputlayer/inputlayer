% Test: Update with computed values

.kg create test_update_computed_n27t34
.kg use test_update_computed_n27t34

+prices[(1, 100), (2, 200), (3, 300)].

?- prices(Id, Price).

% Apply 10% increase
-prices(Id, Price).
+prices(Id, NewPrice) :- prices(Id, Price), NewPrice = Price + Price / 10.

?- prices(Id, Price).

.kg use default
.kg drop test_update_computed_n27t34
