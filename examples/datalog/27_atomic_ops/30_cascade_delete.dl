% Test: Cascade delete pattern
% Expected: Delete dependent rows
% Category: 27_atomic_ops
% Priority: P2 - Atomic operations

.kg create test_cascade
.kg use test_cascade

+parent[(1, "A"), (2, "B")].
+child[(1, 1, "c1"), (2, 1, "c2"), (3, 2, "c3")].

?- parent(Id, Name).
?- child(ChildId, ParentId, Name).

% Delete parent 1
-parent(Id, Name) :- parent(Id, Name), Id = 1.

?- parent(Id, Name).

.kg use default
.kg drop test_cascade
