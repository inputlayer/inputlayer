% Test: Set minus then aggregate

.kg create test_minus_agg_n22t30
.kg use test_minus_agg_n22t30

+all_values[(1,), (2,), (3,), (4,), (5,)].
+exclude_vals[(2,), (4,)].

% Remaining values after exclusion
remaining(X) :- all_values(X), !exclude_vals(X).

% Sum of remaining
sum_remaining(sum<X>) :- remaining(X).

?- remaining(X).
?- sum_remaining(S).

.kg use default
.kg drop test_minus_agg_n22t30
