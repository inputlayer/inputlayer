% Test: Escaped quotes in strings
% Expected: Handle strings with quote characters
% Category: 10_edge_cases
% Priority: P2 - Edge cases

.kg create test_escaped
.kg use test_escaped

+messages[("Hello World",), ("Say \"hi\"",), ("It's fine",)].

?- messages(M).

.kg use default
.kg drop test_escaped
