% Test: Rule with no matching facts

.kg create test_rule_no_match_n10t76
.kg use test_rule_no_match_n10t76

+items[(1, "a"), (2, "b")].

% Rule that won't match anything
impossible(X, Y) :- items(X, Y), X = "string".

?- impossible(X, Y).

.kg use default
.kg drop test_rule_no_match_n10t76
