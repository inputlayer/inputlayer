% Test: Anti-join pattern
% Expected: Find rows not matching in other table
% Category: 10_edge_cases
% Priority: P2 - Edge cases

.kg create test_anti
.kg use test_anti

+all_users[(1, "Alice"), (2, "Bob"), (3, "Carol"), (4, "Dave")].
+active_users[(1,), (3,)].

% Anti-join: users not in active
inactive(Id, Name) :- all_users(Id, Name), !active_users(Id).

?- inactive(Id, Name).

.kg use default
.kg drop test_anti
