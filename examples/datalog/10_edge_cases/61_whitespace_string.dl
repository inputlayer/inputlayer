% Test: Whitespace in strings
% Expected: Strings with spaces handled
% Category: 10_edge_cases
% Priority: P2 - Edge cases

.kg create test_whitespace
.kg use test_whitespace

+names[(1, "John Doe"), (2, "Jane Smith"), (3, "Bob")].

?- names(Id, Name).

% Filter for name with space
has_space(Id, Name) :- names(Id, Name), Name = "John Doe".

?- has_space(Id, Name).

.kg use default
.kg drop test_whitespace
