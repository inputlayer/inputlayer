% Test: Very wide tuple (many columns)
% Expected: High arity tuples work correctly
% Category: 10_edge_cases
% Priority: P2 - Edge cases

.kg create test_wide
.kg use test_wide

% 8-column relation
+wide[(1, "a", 10, 1.5, "x", 100, 2.5, "y")].
+wide[(2, "b", 20, 2.5, "z", 200, 3.5, "w")].

?- wide(A, B, C, D, E, F, G, H).

% Project from wide tuple
narrow(A, C, F) :- wide(A, _, C, _, _, F, _, _).

?- narrow(A, C, F).

.kg use default
.kg drop test_wide
