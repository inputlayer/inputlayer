% Test: Intersection that yields empty
% Expected: No results from disjoint sets
% Category: 10_edge_cases
% Priority: P2 - Edge cases

.kg create test_empty_int
.kg use test_empty_int

+set_a[(1,), (2,), (3,)].
+set_b[(4,), (5,), (6,)].

% Intersection of disjoint sets
common(X) :- set_a(X), set_b(X).

?- common(X).

.kg use default
.kg drop test_empty_int
