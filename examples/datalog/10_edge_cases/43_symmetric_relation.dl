% Test: Symmetric relation pattern

.kg create test_symmetric_n10t43
.kg use test_symmetric_n10t43

+friend[(1, 2), (2, 3), (1, 4)].

% Make symmetric: if (A, B) then also (B, A)
+symmetric(A, B) :- friend(A, B).
+symmetric(A, B) :- friend(B, A).

?- symmetric(A, B).

.kg use default
.kg drop test_symmetric_n10t43
