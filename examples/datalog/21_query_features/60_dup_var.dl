% Test: Duplicate variable in body
% Expected: Same var binds same value
% Category: 21_query_features
% Priority: P2 - Query features

.kg create test_dup_var
.kg use test_dup_var

+pairs[(1, 1), (2, 3), (3, 3), (4, 5)].

% Where first = second
diagonal(X) :- pairs(X, X).

?- diagonal(X).

.kg use default
.kg drop test_dup_var
