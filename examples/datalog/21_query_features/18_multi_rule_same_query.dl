% Test: Multiple rules feeding same query
% Expected: Query sees union of all rule results
% Category: 21_query_features
% Priority: P2 - Query features

.kg create test_multi_rule
.kg use test_multi_rule

+numbers[(1,), (2,), (3,), (4,), (5,)].

% Two clauses contribute to same head
+categorized(X, "small") :- numbers(X), X <= 2.
+categorized(X, "large") :- numbers(X), X > 2.

?- categorized(X, Cat).

.kg use default
.kg drop test_multi_rule
