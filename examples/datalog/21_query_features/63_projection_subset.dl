% Test: Projection to subset of columns
% Expected: Select specific columns
% Category: 21_query_features
% Priority: P2 - Query features

.kg create test_proj_sub
.kg use test_proj_sub

+full[(1, "a", 100, true, "x")].

% Project columns 1, 3, 5
subset(A, C, E) :- full(A, _, C, _, E).

?- subset(A, C, E).

.kg use default
.kg drop test_proj_sub
