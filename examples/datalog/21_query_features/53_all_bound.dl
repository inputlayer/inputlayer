% Test: Query with all bound values
% Expected: Check if specific tuple exists
% Category: 21_query_features
% Priority: P2 - Query features

.kg create test_all_bound
.kg use test_all_bound

+data[(1, "a", 100), (2, "b", 200)].

% Query for exact tuple (exists)
?- data(1, "a", 100).

% Query for non-existent tuple
?- data(1, "a", 999).

.kg use default
.kg drop test_all_bound
