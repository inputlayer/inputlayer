% Test: Query with all bound values

.kg create test_all_bound_n21t53
.kg use test_all_bound_n21t53

+data[(1, "a", 100), (2, "b", 200)].

% Query for exact tuple (exists)
?- data(1, "a", 100).

% Query for non-existent tuple
?- data(1, "a", 999).

.kg use default
.kg drop test_all_bound_n21t53
