% Test: Multi-clause rule

.kg create test_multi_clause_n21t57
.kg use test_multi_clause_n21t57

+dogs[(1, "Buddy"), (2, "Max")].
+cats[(3, "Whiskers"), (4, "Luna")].

% Pets from both sources
+pets(Id, Name) :- dogs(Id, Name).
+pets(Id, Name) :- cats(Id, Name).

?- pets(Id, Name).

.kg use default
.kg drop test_multi_clause_n21t57
