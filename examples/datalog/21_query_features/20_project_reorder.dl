% Test: Reorder columns in projection

.kg create test_proj_reorder_n21t20
.kg use test_proj_reorder_n21t20

+data[(1, "Alice", 100), (2, "Bob", 200)].

% Project with different order
reordered(Name, Val, Id) :- data(Id, Name, Val).

?- reordered(Name, Val, Id).

.kg use default
.kg drop test_proj_reorder_n21t20
