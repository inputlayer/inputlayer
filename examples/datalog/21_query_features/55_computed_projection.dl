% Test: Computed value in projection

.kg create test_computed_proj_n21t55
.kg use test_computed_proj_n21t55

+items[(1, 10, 5), (2, 20, 3)].

% Project computed total
with_total(Id, Total) :- items(Id, Price, Qty), Total = Price * Qty.

?- with_total(Id, T).

.kg use default
.kg drop test_computed_proj_n21t55
