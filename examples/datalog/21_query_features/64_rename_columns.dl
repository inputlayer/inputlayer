% Test: Column renaming via rule

.kg create test_rename_col_n21t64
.kg use test_rename_col_n21t64

+source[(1, "Alice", 100)].

% Rename columns
renamed(UserId, UserName, Balance) :- source(UserId, UserName, Balance).

?- renamed(Id, Name, Bal).

.kg use default
.kg drop test_rename_col_n21t64
