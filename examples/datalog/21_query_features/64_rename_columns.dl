% Test: Column renaming via rule
% Expected: Project with different names
% Category: 21_query_features
% Priority: P2 - Query features

.kg create test_rename_col
.kg use test_rename_col

+source[(1, "Alice", 100)].

% Rename columns
renamed(UserId, UserName, Balance) :- source(UserId, UserName, Balance).

?- renamed(Id, Name, Bal).

.kg use default
.kg drop test_rename_col
