% Test: Zero value handling

.kg create test_zero_n11t60
.kg use test_zero_n11t60

+nums[(0,), (1,), (10,)].

% Zero in arithmetic
computed(N, Plus, Times) :- nums(N), Plus = N + 0, Times = N * 0.

?- computed(N, P, T).

.kg use default
.kg drop test_zero_n11t60
