% Test: Boolean values in facts
% Expected: Handle true/false values
% Category: 11_types
% Priority: P2 - Type handling

.kg create test_bool
.kg use test_bool

+flags[("feature_a", true), ("feature_b", false), ("feature_c", true)].

% Query all flags
?- flags(Name, Value).

% Query enabled flags
enabled(Name) :- flags(Name, true).

?- enabled(Name).

.kg use default
.kg drop test_bool
