% Test: Very small float values

.kg create test_small_floats_n11t54
.kg use test_small_floats_n11t54

+tiny[(1.0e-100,), (2.0e-100,), (3.0e-100,)].

% Arithmetic on tiny floats
+sum_tiny(sum<X>) :- tiny(X).

?- sum_tiny(S).

% Comparison of tiny floats
tiny_cmp(A, B) :- tiny(A), tiny(B), A < B.

?- tiny_cmp(A, B).

.kg use default
.kg drop test_small_floats_n11t54
