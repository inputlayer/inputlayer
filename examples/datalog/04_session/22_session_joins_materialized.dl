.kg create test_join_mat_n04t22
.kg use test_join_mat_n04t22

+edge(1, 2).
+edge(2, 3).
+edge(3, 4).
+label(1, "start").
+label(4, "end").

+reachable(X, Y) :- edge(X, Y).
+reachable(X, Z) :- reachable(X, Y), edge(Y, Z).

path_between_labels(From, To) :- label(From, "start"), label(To, "end"), reachable(From, To).
?- path_between_labels(X, Y).

.kg use default
.kg drop test_join_mat_n04t22
