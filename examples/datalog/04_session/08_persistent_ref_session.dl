% Test: Persistent rule referencing session

.kg create test_pers_ref_n04t08
.kg use test_pers_ref_n04t08

+base[(1,), (2,), (3,)].

% Session rule
temp_double(X, Y) :- base(X), Y = X * 2.

% Try to reference session from persistent
% This tests the semantics
+uses_temp(X, Y) :- temp_double(X, Y).

?- uses_temp(X, Y).

.kg use default
.kg drop test_pers_ref_n04t08
