% Test: Empty Insert
% Category: 12_errors
% Description: Tests handling of empty insert statements

.kg create test_empty
.kg use test_empty

% Empty bulk insert (invalid syntax - should error)
% Note: The correct behavior is to reject empty bulk inserts
% +empty[].  -- This is invalid syntax

% Query the relation (should report undefined relation)
?- empty(X, Y).

% Cleanup
.kg use default
.kg drop test_empty
