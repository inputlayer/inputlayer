% Test: Empty Insert
% Category: 12_errors
% Description: Tests handling of empty insert statements

.kg create test_empty
.kg use test_empty

% Empty bulk insert - should error or be rejected
+empty[].

% If that didn't error, try querying
?- empty(X, Y).

% Cleanup
.kg use default
.kg drop test_empty
