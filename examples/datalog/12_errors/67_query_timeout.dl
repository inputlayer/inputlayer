% Test: Multi-way product query
% Verifies: Large cartesian product (5^4=625 rows) completes correctly

.kg create test_complex_query_n12t67
.kg use test_complex_query_n12t67

+data[(1,), (2,), (3,), (4,), (5,)].

% Multi-way join that creates many combinations
many_ways(A, B, C, D) :- data(A), data(B), data(C), data(D).

+count_ways(count<A>) :- many_ways(A, _, _, _).

?- count_ways(N).

.kg use default
.kg drop test_complex_query_n12t67
