% Test: Cyclic negation (should error)

.kg create test_cyclic_neg_n12t60
.kg use test_cyclic_neg_n12t60

+base[(1,)].

% Cyclic: a depends on !b, b depends on !a
+a(X) :- base(X), !b(X).
+b(X) :- base(X), !a(X).

.kg use default
.kg drop test_cyclic_neg_n12t60
