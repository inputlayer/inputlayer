% Test: Query complexity (timeout proxy test)
% Expected: Complex query completes or errors gracefully
% Category: 12_errors
% Priority: P2 - Error handling

.kg create test_complex_query
.kg use test_complex_query

+data[(1,), (2,), (3,), (4,), (5,)].

% Multi-way join that creates many combinations
many_ways(A, B, C, D) :- data(A), data(B), data(C), data(D).

+count_ways(<COUNT:A>) :- many_ways(A, _, _, _).

?- count_ways(N).

.kg use default
.kg drop test_complex_query
