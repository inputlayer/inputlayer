% Test: Greater-than-or-equal filter
% Expected: Filter by >= comparison
% Category: 07_filters
% Priority: P2 - Filter patterns

.kg create test_ge
.kg use test_ge

+scores[(1, 70), (2, 80), (3, 90), (4, 60)].

% Scores >= 80
passing(Id, S) :- scores(Id, S), S >= 80.

?- passing(Id, S).

.kg use default
.kg drop test_ge
