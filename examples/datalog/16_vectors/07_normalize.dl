% Test: Vector Normalization
% Category: 16_vectors
% Description: Tests normalize(v) function to get unit vectors

.kg create test_vec_norm
.kg use test_vec_norm

% Insert vectors
+vectors[(1, [3.0, 4.0]), (2, [1.0, 0.0]), (3, [0.0, 0.0])].

% Normalize vectors (result should have magnitude 1)
normalized(Id, Norm) :- vectors(Id, V), Norm = normalize(V).

% Query normalized vectors
?- normalized(Id, Norm).

.kg use default
.kg drop test_vec_norm
