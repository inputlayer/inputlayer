// Test: Negation with Empty Result
// Category: 08_negation
// Description: Tests negation where all tuples are filtered out

.db create test_neg_empty
.db use test_neg_empty

// All edges
+edge[(1,2), (2,3), (3,4)].

// Block all edges
+blocked[(1,2), (2,3), (3,4)].

// Filter - should return empty since all edges are blocked
allowed(X, Y) := edge(X, Y), !blocked(X, Y).

// Expected: No results (all filtered)
?- allowed(X, Y).

// Cleanup
.db use default
.db drop test_neg_empty
