% Test: Session Clear Command
% Description: Tests .session clear to remove all session rules

.kg create test_session_clear_n33t07
.kg use test_session_clear_n33t07

% Insert some data
+edge[(1, 2), (2, 3)].

% Create session rules
connected(X, Y) :- edge(X, Y).
path(X, Y) :- edge(X, Y).

% Show current rules
.session

% Clear all session rules
.session clear

% Show empty session
.session

% Cleanup
.kg use default
.kg drop test_session_clear_n33t07
