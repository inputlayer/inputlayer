% Test: Session drop with invalid index

.kg create test_sess_invalid_n33t08
.kg use test_sess_invalid_n33t08

% Add a session rule
test_rule(X) :- items(X).

% List sessions
.session

% Try to drop invalid index (too high)
.session drop 99

% Try to drop index 0 (should be invalid if 1-based)
.session drop 0

.kg use default
.kg drop test_sess_invalid_n33t08
