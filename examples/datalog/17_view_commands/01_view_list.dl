// Test: View List Command
// Category: 17_view_commands
// Description: Tests .view command to list all defined views

.db create test_view_list
.db use test_view_list

// Insert base data
+edge[(1,2), (2,3)].

// Define multiple views
view reverse(X: int, Y: int) :- edge(Y, X).
view paths(X: int, Y: int) :- edge(X, Y).
view paths(X: int, Z: int) :- paths(X, Y), edge(Y, Z).

// List all views
.view

// Cleanup
.db use default
.db drop test_view_list
