// Test: View Describe Command
// Category: 17_view_commands
// Description: Tests .view <name> to describe a specific view

.db create test_view_desc
.db use test_view_desc

// Insert base data
+edge[(1,2), (2,3), (3,4)].

// Define a view with multiple rules
view reach(X: int, Y: int) :- edge(X, Y).
view reach(X: int, Z: int) :- reach(X, Y), edge(Y, Z).

// Describe the view
.view reach

// Cleanup
.db use default
.db drop test_view_desc
