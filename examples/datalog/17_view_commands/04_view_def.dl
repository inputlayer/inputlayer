// Test: View Definition Command
// Category: 17_view_commands
// Description: Tests .view def <name> to show only definition

.db create test_view_def
.db use test_view_def

// Insert base data
+edge[(1,2), (2,3)].

// Define a view
reach(X, Y) := edge(X, Y).
reach(X, Z) := reach(X, Y), edge(Y, Z).

// Show definition only
.view def reach

// Cleanup
.db use default
.db drop test_view_def
