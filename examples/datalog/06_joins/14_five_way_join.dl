% Test: Five-way join
% Expected: Complex five-table join works
% Category: 06_joins
% Priority: P2 - Complex joins

.kg create test_five_join
.kg use test_five_join

+a[(1, 10), (2, 20)].
+b[(10, 100), (20, 200)].
+c[(100, 1000), (200, 2000)].
+d[(1000, 10000), (2000, 20000)].
+e[(10000, "final"), (20000, "done")].

% Five-way join
five_join(A, B, C, D, E) :- a(X, A), b(A, B), c(B, C), d(C, D), e(D, E).

?- five_join(A, B, C, D, E).

.kg use default
.kg drop test_five_join
