% Test: Anti-join pattern
% Expected: Find items not in another set
% Category: 06_joins
% Priority: P2 - Join patterns

.kg create test_anti_join
.kg use test_anti_join

+all_users[(1, "Alice"), (2, "Bob"), (3, "Carol"), (4, "Dave")].
+active_users[(1,), (3,)].

% Find inactive users (anti-join)
+inactive(Id, Name) :- all_users(Id, Name), !active_users(Id).

?- inactive(Id, Name).

.kg use default
.kg drop test_anti_join
