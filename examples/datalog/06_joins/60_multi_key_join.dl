% Test: Multi-key join
% Expected: Join on multiple columns
% Category: 06_joins
% Priority: P2 - Join patterns

.kg create test_multi_key
.kg use test_multi_key

+table_a[(1, "x", "A"), (1, "y", "B"), (2, "x", "C")].
+table_b[(1, "x", 100), (1, "y", 200), (3, "z", 300)].

% Join on first two columns
+joined(K1, K2, ValA, ValB) :- table_a(K1, K2, ValA), table_b(K1, K2, ValB).

?- joined(K1, K2, A, B).

.kg use default
.kg drop test_multi_key
