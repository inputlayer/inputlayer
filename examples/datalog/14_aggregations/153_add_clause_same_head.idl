// Test: Add second clause with same head, different body (union semantics)
// Description: Adding a second clause to an existing rule should produce union, not replacement

.kg create test_add_clause_n14t153
.kg use test_add_clause_n14t153

+edge[(1, 2), (2, 3), (3, 4)]
+shortcut[(1, 4)]

+reach(X, Y) <- edge(X, Y)

?reach(X, Y)

// ADD second clause - NOT a replacement
+reach(X, Y) <- shortcut(X, Y)

// Should return 4 rows (union of both clauses)
?reach(X, Y)

// Should show reach with 2 clauses
.rule list

.kg use default
.kg drop test_add_clause_n14t153
