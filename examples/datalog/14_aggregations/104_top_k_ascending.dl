% Test: Top-K Ascending (Bottom-K)
% Description: Tests top_k without desc flag - returns bottom K items

.kg create test_top_k_asc_n14t104
.kg use test_top_k_asc_n14t104

% Insert prices: price(item_id, cost)
+price[(1, 99), (2, 250), (3, 50), (4, 300), (5, 75), (6, 200)].

% Get 3 cheapest items (ascending order - no desc flag)
+cheapest(top_k<3, Item, Cost:asc>) :- price(Item, Cost).

?- cheapest(X, Y).

% Cleanup
.kg use default
.kg drop test_top_k_asc_n14t104
