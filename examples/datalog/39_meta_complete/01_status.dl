% Test: .status command
% Description: Tests the .status meta command exists and runs without error
% NOTE: The output contains dynamic values (uptime, query count) that vary
%       so we just verify the command runs without error

.kg create test_status_n39t01
.kg use test_status_n39t01

% Insert some data
+data[(1, "a"), (2, "b"), (3, "c")].

% Verify data accessible
?- data(Id, Val).

.kg use default
.kg drop test_status_n39t01
