% Test: Schema Arity Validation
% Category: 24_rel_schemas
% Description: Tests that relations enforce arity constraints

.kg create test_arity
.kg use test_arity

% Define a relation with explicit schema (3 columns)
+employees(id: int, name: string, dept: string).

% Insert valid 3-column data
+employees[(1, "Alice", "Engineering"), (2, "Bob", "Sales")].

% Query the data
?- employees(Id, Name, Dept).

% Show relation info
.rel employees

% Define another relation with 2 columns
+projects(id: int, name: string).

% Insert valid 2-column data
+projects[(100, "ProjectA"), (101, "ProjectB")].

% Query the data
?- projects(Id, Name).

% List all relations
.rel

% Cleanup
.kg use default
.kg drop test_arity
