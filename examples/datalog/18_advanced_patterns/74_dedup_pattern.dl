% Test: Deduplication pattern
% Expected: Keep only unique combinations
% Category: 18_advanced_patterns
% Priority: P2 - Advanced patterns

.kg create test_dedup
.kg use test_dedup

+events[(1, "click"), (2, "click"), (3, "view"), (4, "click"), (5, "view")].

% Get unique event types
unique_types(T) :- events(_, T).

?- unique_types(T).

.kg use default
.kg drop test_dedup
