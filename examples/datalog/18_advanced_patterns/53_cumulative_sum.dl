% Test: Cumulative sum pattern

.kg create test_cumsum_n18t53
.kg use test_cumsum_n18t53

+daily[(1, 10), (2, 20), (3, 15), (4, 25)].

% Cumulative sum up to each day
+cumsum(Day, sum<Amt>) :- daily(D, Amt), D <= Day, daily(Day, _).

?- cumsum(Day, Total).

.kg use default
.kg drop test_cumsum_n18t53
