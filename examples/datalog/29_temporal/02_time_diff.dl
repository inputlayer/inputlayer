% Test: time_diff(t1, t2) function
% Description: Tests time_diff returns difference between timestamps in milliseconds

.kg create test_time_diff_n29t02
.kg use test_time_diff_n29t02

% Insert pairs of timestamps
+timestamps[(1, 1000, 500), (2, 2000, 1500), (3, 1000, 1000)].

% Define rule to compute time difference
result(Id, Diff) :- timestamps(Id, T1, T2), Diff = time_diff(T1, T2).

% Query the results
?- result(Id, Diff).

.kg use default
.kg drop test_time_diff_n29t02
