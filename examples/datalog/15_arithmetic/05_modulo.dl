% Test: Modulo Computed Column
% Description: Tests modulo in rule head

.kg create test_arith_mod_n15t05
.kg use test_arith_mod_n15t05

% Insert numbers: num(id, value)
+num[(1, 17), (2, 24), (3, 35), (4, 12)].

% Compute value modulo 5
remainder(Id, Value%5) :- num(Id, Value).

% Query remainders
?- remainder(X, Y).

% Cleanup
.kg use default
.kg drop test_arith_mod_n15t05
