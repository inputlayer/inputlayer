% Test: Arithmetic precedence

.kg create test_prec_n15t42
.kg use test_prec_n15t42

+val[(5,)].

% 5 + 3 * 2 should be 11, not 16
result(V, R) :- val(V), R = V + 3 * 2.

?- result(V, R).

.kg use default
.kg drop test_prec_n15t42
