// Test: Basic operations in meta_complete category
// Description: Tests basic operations to ensure category works

.kg create test_meta_basic_n39t02
.kg use test_meta_basic_n39t02

// Create data
+data[(1, "a"), (2, "b")]
?data(X, Y)

// Create a rule
+derived(X, Y) <- data(X, Y)
?derived(X, Y)

.kg use default
.kg drop test_meta_basic_n39t02
