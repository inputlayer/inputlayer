// Test: Negation with No Matches to Filter
// Description: Tests negation where negated relation has no matching tuples

.kg create test_neg_nomatch_n08t04
.kg use test_neg_nomatch_n08t04

// Edges
+edge[(1,2), (2,3), (3,4)]

// Blocked edges that don't overlap with edge
+blocked[(10,20), (30,40)]

// Filter - should return all edges since none are blocked
+allowed(X, Y) <- edge(X, Y), !blocked(X, Y)

?allowed(X, Y)

// Cleanup
.kg use default
.kg drop test_neg_nomatch_n08t04
