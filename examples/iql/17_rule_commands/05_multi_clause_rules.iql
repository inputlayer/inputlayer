// Test: Multi-Clause Rules
// Description: Tests rules with multiple clauses and inspecting them

.kg create test_multi_clause_n17t05
.kg use test_multi_clause_n17t05

// Insert base data
+edge[(1,2), (2,3), (3,4), (4,5)]

// Define a rule with multiple clauses (transitive closure)
+reachable(X, Y) <- edge(X, Y)
+reachable(X, Z) <- reachable(X, Y), edge(Y, Z)

// List all rules (shows clause count)
.rule list

// Show definition of multi-clause rule
.rule def reachable

// Query the rule (shows definition and results)
.rule reachable

// Cleanup
.kg use default
.kg drop test_multi_clause_n17t05
