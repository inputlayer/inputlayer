// Test: Multi-hop recursion with intermediate nodes

.kg create test_multi_hop_n09t14
.kg use test_multi_hop_n09t14

// Direct connections and via intermediate
+direct[(1, 2), (2, 3), (3, 4), (4, 5)]
+hop[(1, 3), (2, 4), (3, 5)]

// Find all paths: either direct or via intermediate
+path(X, Y) <- direct(X, Y)
+path(X, Y) <- hop(X, Y)
+path(X, Z) <- path(X, Y), direct(Y, Z)

?path(1, Y)

.kg use default
.kg drop test_multi_hop_n09t14
