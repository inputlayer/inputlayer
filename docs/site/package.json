{
  "name": "inputlayer-docs-site",
  "version": "0.1.0",
  "description": "InputLayer documentation site (GitHub Pages)",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf pages && cp -r ../content pages",
    "build": "next build",
    "dev": "rm -rf pages && cp -r ../content pages && next dev",
    "postbuild": "next export"
  },
  "dependencies": {
    "next": "^13.5.6",
    "nextra": "^2.13.2",
    "nextra-theme-docs": "^2.13.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "shiki": "^0.14.7"
  },
  "devDependencies": {
    "@types/node": "^18.11.10",
    "@types/react": "^18.2.0",
    "typescript": "^4.9.5"
  }
}
