# Pragmas

InputLayer does not use pragma-based feature flags. All language features (negation, arithmetic, aggregations, etc.) are enabled by default.

See [Extensions](extensions.md) for InputLayer-specific features.
